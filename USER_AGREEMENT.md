# netool 用户许可协议

**版本 1 · 生效日期：2026-07-27**

使用 netool 懒人工具箱（以下简称「本工具」）即表示您已阅读并同意本协议。

## 1. 工具性质

本工具为命令行运维脚本集合，按「现状」提供，旨在辅助 Linux 服务器日常维护。作者不对适用性、完整性或持续可用性作任何明示或暗示保证。

## 2. 系统变更风险

部分功能会修改系统配置，包括但不限于：

- 软件源 / 包管理器配置
- SSH 端口与防火墙规则
- 内核网络参数（如 BBR）
- 主机名、时区、Swap、Docker 等

**请在生产环境操作前备份相关配置**，并使用 `--plan` / `--dry-run` 预览。

## 3. 破坏性操作

以下操作可能导致**不可逆数据丢失**：

- 磁盘全盘读写验盘（`disk-test` / `full-rw-test`）
- 整盘擦除（`wipe`）
- 误选磁盘分区/格式化

执行前必须完成预览并输入确认短语。因误操作导致的数据损失由操作者自行承担。

## 4. 权限与合规

- 改系统配置通常需要 root 或 sudo 权限。
- 请确保您对目标服务器拥有合法运维权限。
- 不得将本工具用于未授权访问或违法用途。

## 5. 远程运行

通过 `curl` 从 Gitee 下载脚本时，您需自行评估网络与源站可信度。本工具会对下载脚本做 `bash -n` 语法校验，但**不能替代完整的安全审计**。

## 6. 免责声明

在法律允许的最大范围内，作者不对因使用或无法使用本工具造成的任何直接、间接、附带或 consequential 损害负责。

## 7. 拒绝协议

若不同意本协议，请立即停止使用并删除已下载的文件。

---

仓库：https://gitee.com/suser747/netool
