# netool 懒人工具箱用户许可协议

使用 netool 懒人工具箱前，请确认你已经理解以下事项。

## 1. 脚本用途

netool 懒人工具箱用于辅助 Linux 系统信息查看、网络排查、Docker 状态查看、软件源切换、SSH 端口管理、磁盘管理和硬盘检测等操作。

## 2. 风险提示

- 部分功能会修改系统配置，例如软件源、SSH 配置、磁盘分区、挂载配置等。
- `disk-test` 硬盘好坏检测会对目标硬盘执行全盘写入和读回校验，会清空被测硬盘上的全部数据。
- `disk wipe` 擦盘功能同样会破坏数据，真正执行必须显式指定 `--devices` 目标盘。
- 在生产服务器、含业务数据的硬盘、远程不可接管服务器上执行高风险功能前，请先完成备份并确认救援方式。

## 3. 用户责任

你需要确认自己有权限管理目标服务器，并对输入的设备路径、端口、镜像源、容器名等参数负责。

执行任何破坏性操作前，请仔细阅读脚本显示的目标预览和风险提示。对硬盘检测类操作，建议先运行：

```bash
bash <(curl -fsSL https://gitee.com/suser747/netool/raw/m/i) disk-test --plan
```

真正执行全盘写入检测时，脚本会要求输入确认短语 `DESTROY-DISK-TEST`。

擦盘前请先预览明确目标盘：

```bash
bash <(curl -fsSL https://gitee.com/suser747/netool/raw/m/i) disk wipe --plan --devices /dev/sdX
```

## 4. 隐私与数据安全

- 本工具仅在本地运行，不会向任何第三方服务器上传数据。
- 审计日志只记录操作类型和时间，**不会记录任何密码、密钥、令牌或其他敏感数据**。
- 审计日志文件权限为 600，目录权限为 700，仅 root 用户可访问。
- 你可以随时通过 `uninstall` 命令删除工具箱的所有配置和日志。

## 5. 免责声明

本工具按现状提供，不承诺适用于所有系统环境。因误操作、系统差异、网络异常、硬盘故障、业务中断或数据丢失造成的损失，由执行者自行承担。

如果不同意以上条款，请不要运行本工具。
